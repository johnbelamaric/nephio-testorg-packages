apiVersion: v1
kind: ConfigMap
metadata:
  name: kptfile.kpt.dev
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  name: example
  upstreamRepo: demo-packages
  downstreamRepo: mgmt
  stagingRepo: mgmt-staging
